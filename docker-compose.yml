version: "2.4"

services:
  app:
    image: jetwheelreelpointca:1.4.8
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=development