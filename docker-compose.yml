version: "3.8"

services:
  app:
    image: superklok/jetwheelreelpointca:1.5.9
    build: 
      context: .
    ports:
      - "80:80"
    command: node app.js
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production